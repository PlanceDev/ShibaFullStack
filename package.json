{
  "name": "shiba",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install && npm run build",
    "dev": "cd backend && nodemon index.ts"
  },
  "dependencies": {
    "copyfiles": "^2.4.1"
  }
}
